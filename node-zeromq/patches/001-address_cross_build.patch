--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -137,3 +137,44 @@ endif()
 if(WIN32)
   target_link_libraries(addon PRIVATE "ShLwApi.lib" "delayimp.lib")
 endif()
+
+# OpenWrt Cross Build
+set(ENABLE_CROSS_COMPILING ON)
+set(CMAKE_CROSSCOMPILING "true")
+set(CMAKE_SYSTEM_NAME "$ENV{CMAKE_SYSTEM_NAME}")
+set(CMAKE_SYSTEM_VERSION "$ENV{CMAKE_SYSTEM_VERSION}")
+set(CMAKE_SYSTEM_PROCESSOR "$ENV{CMAKE_SYSTEM_PROCESSOR}")
+set(CMAKE_BUILD_TYPE "$ENV{CMAKE_BUILD_TYPE}")
+set(CMAKE_C_FLAGS_RELEASE "$ENV{CMAKE_C_FLAGS_RELEAS}")
+set(CMAKE_CXX_FLAGS_RELEASE "$ENV{CMAKE_CXX_FLAGS_RELEASE}")
+set(CMAKE_C_COMPILER_LAUNCHER "$ENV{CMAKE_C_COMPILER_LAUNCHER}")
+set(CMAKE_C_COMPILER "$ENV{CMAKE_C_COMPILER}")
+set(CMAKE_C_COMPILER_ARG1 "$ENV{CMAKE_C_COMPILER_ARG1}")
+set(CMAKE_CXX_COMPILER_LAUNCHER "$ENV{CMAKE_CXX_COMPILER_LAUNCHER}")
+set(CMAKE_CXX_COMPILER "$ENV{CMAKE_CXX_COMPILER}")
+set(CMAKE_CXX_COMPILER_ARG1 "$ENV{CMAKE_CXX_COMPILER_ARG1}")
+set(CMAKE_ASM_COMPILER "$ENV{CMAKE_ASM_COMPILER}")
+set(CMAKE_EXE_LINKER_FLAGS "$ENV{CMAKE_EXE_LINKER_FLAGS}")
+set(CMAKE_MODULE_LINKER_FLAGS "$ENV{CMAKE_MODULE_LINKER_FLAGS}")
+set(CMAKE_SHARED_LINKER_FLAGS "$ENV{CMAKE_SHARED_LINKER_FLAGS}")
+set(CMAKE_AR "$ENV{CMAKE_AR}")
+set(CMAKE_NM "$ENV{CMAKE_NM}")
+set(CMAKE_RANLIB "$ENV{CMAKE_RANLIB}")
+set(CMAKE_FIND_ROOT_PATH "$ENV{CMAKE_FIND_ROOT_PATH}")
+set(CMAKE_FIND_ROOT_PATH_MODE_PROGRAM "$ENV{CMAKE_FIND_ROOT_PATH_MODE_PROGRAM}")
+set(CMAKE_FIND_ROOT_PATH_MODE_LIBRARY "$ENV{CMAKE_FIND_ROOT_PATH_MODE_LIBRARY}")
+set(CMAKE_FIND_ROOT_PATH_MODE_INCLUDE "$ENV{CMAKE_FIND_ROOT_PATH_MODE_INCLUDE}")
+set(DL_LIBRARY "$ENV{DL_LIBRARY}")
+set(CMAKE_SKIP_RPATH "$ENV{CMAKE_SKIP_RPATH}")
+set(CMAKE_EXPORT_PACKAGE_REGISTRY "$ENV{CMAKE_EXPORT_PACKAGE_REGISTRY}")
+set(CMAKE_EXPORT_NO_PACKAGE_REGISTRY "$ENV{CMAKE_EXPORT_NO_PACKAGE_REGISTRY}")
+set(CMAKE_FIND_USE_PACKAGE_REGISTRY "$ENV{CMAKE_FIND_USE_PACKAGE_REGISTRY}")
+set(CMAKE_FIND_PACKAGE_NO_PACKAGE_REGISTRY "$ENV{CMAKE_FIND_PACKAGE_NO_PACKAGE_REGISTRY}")
+set(CMAKE_FIND_USE_SYSTEM_PACKAGE_REGISTRY "$ENV{CMAKE_FIND_USE_SYSTEM_PACKAGE_REGISTRY}")
+set(CMAKE_FIND_PACKAGE_NO_SYSTEM_PACKAGE_REGISTRY "$ENV{CMAKE_FIND_PACKAGE_NO_SYSTEM_PACKAGE_REGISTRY}")
+set(CMAKE_SYSROOT "$ENV{CMAKE_SYSROOT}")
+set(CROSS_COMPILE "$ENV{CROSS_COMPILE}")
+set(CMAKE_OBJCOPY "$ENV{TARGET_CROSS}objcopy")
+set(CMAKE_STRIP "$ENV{TARGET_CROSS}strip")
+set(CMAKE_DISABLE_FIND_BSD ON)
+set(WITH_LIBBSD OFF)
